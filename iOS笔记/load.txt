类和分类同时重写load方法:
+load的执行顺序是先类，后category

只有分类重写load方法:
先调用category +load方法，后类

load 方法调用顺序？
规则一:父类先于子类调用 
规则二:类先于分类调用


它只是一个在整个文件被加载到运行时，在 main 函数调用之前被 ObjC 运行时调用的钩子方法。其中关键字有这么几个：
文件刚加载
main 函数之前
钩子方法
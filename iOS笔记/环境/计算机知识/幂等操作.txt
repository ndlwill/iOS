“幂等”（idempotent）是一个很重要、也很常用的工程概念

幂等操作 = 同一个操作，不管执行 1 次还是执行 N 次，最终结果都一样

manager.isEnabled = true
try await manager.saveToPreferences()
执行 1 次 → enabled = true
执行 10 次 → enabled 还是 true
不会因为重复调用或顺序不同而出问题


非幂等操作（容易出逻辑问题）
manager.onDemandRules.append(rule)
try await manager.saveToPreferences()
执行 1 次 → 1 条规则
执行 2 次 → 2 条重复规则

非幂等（顺序决定结果）
A: server = "vpn1.example.com"
B: server = "vpn2.example.com"
A 先 save → B 后 save → 最终是 vpn2
B 先 save → A 后 save → 最终是 vpn1
👉 这就是你前面说的“逻辑错误”


幂等
setVolume(50)
setVolume(50)
非幂等
increaseVolume(by: 10)
increaseVolume(by: 10)
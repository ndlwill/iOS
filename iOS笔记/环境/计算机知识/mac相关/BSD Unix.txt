OpenBSD 和 FreeBSD 都是“真正的 BSD Unix 系统”，它们是 BSD Unix 的后代


macOS 的核心：Darwin
macOS 是基于一个叫做 Darwin 的开源操作系统。
Darwin 本身 = XNU 内核（混合内核） + BSD 用户空间（主要借自 FreeBSD）
所以 macOS 内部用了很多 FreeBSD 的网络协议栈、用户命令、文件系统接口等。


它也不是“纯 BSD”，而是融合了 BSD、Mach、NeXT 的混合体。
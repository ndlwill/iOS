mbedTLS（前身为PolarSSL）和OpenSSL是两个常用的开源加密库

mbedTLS： 设计简单、轻量，适用于嵌入式系统和资源受限环境。它的目标是提供基本的加密和安全功能，同时尽量减小代码的体积。
OpenSSL： 功能更为丰富，支持广泛的加密算法和协议，适用于各种环境，包括嵌入式系统和大型服务器。它是一个强大而全面的加密库，功能涵盖从对称加密到非对称加密，再到证书管理等多个方面。

在选择加密库时，需要根据具体的需求和项目要求权衡这些因素。如果是嵌入式系统或对资源敏感，可能更倾向于使用mbedTLS；而在需要更多功能和更大社区支持的情况下，OpenSSL可能更为适用。
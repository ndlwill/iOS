https://juejin.cn/post/7231731488928399415


渲染流程（Render loop）:
在我们的app中，每一帧的渲染分为三步。
第一步，我们操作app会触发UI变更。
第二步，我们操作的变更会被打包提交到独立的Render server（渲染服务）进程进行渲染，这个阶段UI才被真正渲染。
第三步，将这一帧显示出来。
这三步中的每一步都需要在一个VSync内完成，否则就会出现丢帧。

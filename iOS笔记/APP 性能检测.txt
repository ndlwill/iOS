启动时间
内存占用量，内存告警次数
CPU使用率
页面渲染时间，刷新帧率
网络请求时间，流量消耗
UI阻塞次数，不可操作时长，主线程阻塞超过400毫秒次数
耗电功率

对于静态页面来讲，页面的渲染时间就是从viewDidLoad第一行到viewDidAppear最后一行代码的时间

主线程阻塞超过400毫秒就会让用户感知到卡顿

AOP(面向切面编程)：采用切面的方式，统一的为大量的类增加检测代码

Xcode在debug模式下是没有开启watch dog的

========================================
查找 App 中的内存问题，比如内存泄露(Leaked memory)、废弃内存(Abandoned memory)、僵尸(zombies)等
exit()退出

要运行代码，就需要Python解释器去执行.py文件
方版本的解释器：CPython

========================Http
HTTP 请求由3部分组成，分别是请求行、请求首部（Header）、请求体，首部和请求体是可选的
请求行是每个请求必不可少的部分，它由3部分组成，分别是请求方法（method)、请求URL（URI）、HTTP协议版本，以空格隔开。
GET 方法用于从服务器获取资源，90%的爬虫都是基于GET请求抓取数据。
请求首部用于给服务器提供一些额外的信息，比如 User-Agent 用来表明客户端的身份，让服务器知道你是来自浏览器的请求还是爬虫，是来自 Chrome 浏览器还是 FireFox。
因为客户端发送请求时，发送的数据（报文）是由字符串构成的，为了区分请求首部的结尾和请求体的开始，用一个空行来表示，遇到空行时，就表示这是首部的结尾，请求体的开始。
请求体是客户端提交给服务器的真正内容

HTTP 响应也由3部分组成，分别是：响应行、响应首部、响应体
响应行同样也是3部分组成，由服务端支持的 HTTP 协议版本号、状态码、以及对状态码的简短原因描述组成
如果是500，说明服务器处理请求的时候出现了异常。404 表示请求的资源在服务器找不到
响应首部可以告知客户端响应体的数据类型是什么
响应体（body）是服务器返回的真正内容，它可以是一个HTML页面，或者是一张图片、一段视频等等。

========================直接运行.py文件
#!/usr/bin/env python3
print('hello, world') // 用单引号或者双引号括起来的文本在程序中叫字符串

通过命令给hello.py以执行权限：
$ chmod a+x hello.py

./hello.py 

========================
print('The quick brown fox', 'jumps over', 'the lazy dog')
遇到逗号“,”会输出一个空格
print('100 + 200 =', 100 + 200)

Python程序是大小写敏感的

以#开头的语句是注释

当语句以冒号:结尾时，缩进的语句视为代码块

用4个空格的缩进